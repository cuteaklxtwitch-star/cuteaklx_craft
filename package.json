{
  "name": "mwc-render-server",
  "private": true,
  "version": "1.0.0",
  "description": "Runtime server: built web client + WebSocket->TCP Minecraft proxy with Microsoft login, one process, one port",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "express": "^5.2.1",
    "mwc-proxy": "^0.0.4"
  }
}
